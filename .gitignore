__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

node_modules/
frontend/dist/

.env
.DS_Store
*.swp

# Agent skills / local dev tooling (kept on disk, not tracked)
.agents/
.claude/
skills-lock.json

# template preview build artifacts (throwaway)
letters/public/_tpl_preview/

# pytest artifact: renderers mkdir a MagicMock() get_site_path() when frappe is
# mocked outside a bench (see block_renderers/social.py)
MagicMock/

# local Python 3.11 venv (system python3 is 3.9, repo requires >=3.10)
.venv311/
